pipeline {
    agent any

    tools {


    stages {
        stage('Install Dependencies') {
            steps {
                sh '''
                    npm install --no-audit
                    
                '''
            }
        }
    }
}

